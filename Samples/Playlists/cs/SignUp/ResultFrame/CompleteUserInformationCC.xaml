<Page x:Class="SDKTemplate.SignUp.CompleteUserInformationCC"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:SDKTemplate.SignUp"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBlock x:Name="location" />
            <TextBlock Text="{x:Bind _CUIV.HSAV.MobileNo, Mode=OneWay}" />
            <TextBlock Text="{x:Bind _CUIV.PCV.Name, Mode=OneWay}" />
            <TextBlock Text="{x:Bind _CUIV.PCV.EmailId, Mode=OneWay}" />
            <TextBlock Text="{x:Bind _CUIV.PCV.DateOfBirth, Mode=OneWay}" />
            <TextBlock Text="{x:Bind _CUIV.BIV.BusinessName, Mode=OneWay}" />
            <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                <WebView x:Name="MapWebView"
                         Width="Auto"
                         Height="400" />
            </Grid>
            <TextBlock Text="{x:Bind _CUIV.BIV.FullAddress, Mode=OneWay}" />
            <TextBlock Text="{x:Bind _CUIV.BIV.Category[0], Mode=OneWay}" />
        </StackPanel>
    </Grid>
</Page>
