<Page x:Class="SDKTemplate.PaymentOptionCC"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:SDKTemplate"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">
    <UserControl.Resources>
        <local:BooleanConverter x:Key="BooleanConverter" />
    </UserControl.Resources>
    <StackPanel x:Name="MasterStackPanel"
                Background="White"
                HorizontalAlignment="Left"
                Margin="10,0">
        <TextBlock TextWrapping="Wrap"
                   Style="{StaticResource SubtitleTextBlockStyle}"
                   Text="Pay With"
                   Width="400" />
        <ListView x:Name="PaymentOptionListView" ItemsSource="{x:Bind PaymentOptions, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:PaymentOptionViewModel">
                    <RadioButton Content="{x:Bind PaymentOption.Name}"
                                 IsChecked="{x:Bind IsChecked, Mode=TwoWay}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
