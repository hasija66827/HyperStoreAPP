<Page x:Class="SDKTemplate.ProductConsumptionPer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:SDKTemplate"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:Charting="using:WinRTXamlToolkit.Controls.DataVisualization.Charting"
      mc:Ignorable="d">
    <RelativePanel Background="White">
        <Border  x:Name="HeaderPanel"
                 RelativePanel.AlignTopWithPanel="True"
                 Style="{StaticResource HeaderBorder}">
            <TextBlock Text="Product Consumption Trend"
                       Style="{StaticResource HeaderTextBlockStyle}" />
        </Border>
        <Charting:Chart x:Name="ColumnChart"
                        RelativePanel.Below="HeaderPanel"
                        HorizontalAlignment="Left"
                        Margin="0"
                        Width="350">
            <Charting:ColumnSeries IndependentValuePath="FormattedDay"
                                   DependentValuePath="AvgConsumption"
                                   CharacterSpacing="4"
                                   IsSelectionEnabled="True" />
            <Charting:ColumnSeries IndependentValuePath="FormattedDay"
                                   DependentValuePath="SecondSeriesHits" />
        </Charting:Chart>
    </RelativePanel>
</Page>
